<%=
  tr_tag(
    model,
    {
      href: url_for(controller: 'app_models', app_id: model.id),
      icon: 'list',
      title: t('.app_models.index.title')
    },
    data: {
      url: url_for(action: 'reorder', id: model.id),
      id: model.id
    }
  ) { yield }
%>
